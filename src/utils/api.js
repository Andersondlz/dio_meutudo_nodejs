export const key = {
    value: '12345',
    permission: 'admin access',
    type: 'API Key'
}

export const getDataFromAPI = async (endpoint) => {
    // Simulate an API call
    console.log(`fetching data from API endpoint... "${endpoint}"` );
}